import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ulmeanu.radioplaygroundstream',
  appName: 'Radio Playground Stream',
  webDir: 'build'
};

export default config;
